# Runtime dependencies (a copy of [project.dependencies] in pyproject.toml, with the
# observer/task engine pinned to the archived release this study was run against).
#
#   pip install -r requirements.txt
#
numpy>=1.24
scipy>=1.10
scikit-image>=0.20
matplotlib>=3.7
taskiq-core @ git+https://github.com/Institute-of-One/taskiq-core@v0.4.0
